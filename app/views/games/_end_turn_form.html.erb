<%= form_tag(game_path, method: :patch) do %>
  <%= hidden_field_tag(:state, state) %>
  <div class="field">
    <div class="control">
      <%= submit_tag("End turn", class: 'button is-link') %>
    </div>
  </div>
<% end %>
