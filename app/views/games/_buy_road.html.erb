<%= form_tag(game_path, method: :patch) do %>
  <%= hidden_field_tag(:state, state) %>
  <div class="columns">
    <div class="column">
      <div class="field is-horizontal">
        <div class="field-body">
          <div class="field">
            <div class="control">
              <%= submit_tag("Buy road", class: 'button is-link', disabled: disabled) %>
            </div>
          </div>
          <div class="field">
            <div class="control">
              <%= text_field(:road, :from, class: 'input', required: true, disabled: disabled, placeholder: 'Spot 1') %>
            </div>
          </div>
          <div class="field">
            <div class="control">
              <%= text_field(:road, :to, class: 'input', required: true, disabled: disabled, placeholder: 'Spot 2') %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
